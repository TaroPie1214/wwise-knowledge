# 版本说明 2023.1.7

|  |
| --- |
| Wwise SDK 2025.1.4 - Windows |

版本说明 2023.1.7

以下各节列举并阐述了 2023.1.6 和 2023.1.7 版本之间针对 Wwise 所作的改进。  
此处提供了有关平台的特定信息：

[Windows 2023.1.7](windows_releasenotes_2023_1_7.html)

有关早期版本的信息，请参阅 [过往版本的发行说明](previousreleases.html) 章节。

- [新增功能](whatsnew_2023_1_7.html#generic_feature_changes_2023_1_7)
- [漏洞修复](whatsnew_2023_1_7.html#generic_bugs_2023_1_7)
- [社区报告的漏洞修复](whatsnew_2023_1_7.html#generic_community_bugs_2023_1_7)

# 新增功能

- **WG-68107** (wp.py) 现在允许在构建后暴露参数并传给 `SignTool.exe` 来在 Windows 上对动态库进行签名。
- **WG-69592** 添加了通过 wp.py 对特定工具集执行 premake 操作的选项。

# 漏洞修复

- **WG-71240** 已修复：当音频引擎初始化时将 `AkInitSettings::bUseSoundBankMgrThread` 设置为 `false` 时，函数 `LoadBank` 和 `UnloadBank` 不调用用户回调。
- **WG-73008** 已修复：录音机插件输出文件头部的未定义数据。
- **WG-73069** 已修复：(空间音频) 房间传输损失忽略没有几何体的父房间。
- **WG-73475** 已修复：在**内容编辑器**或**列表视图**中使用**响度**或**持续时间**列进行排序时可能崩溃。
- **WG-73487** 已修复：波形显示无法正确刷新。
- **WG-73491** 已修复：在使用浮点输入源重新采样时内存不足导致发生崩溃。
- **WG-73552** 已修复：(Spatial Audio) 某些距离探针场景中的距离计算不准确。
- **WG-73617** 已修复：在对 Vorbis 音频源进行解码时内存不足导致内存泄漏。
- **WG-73741** 已修复：潜在的内存损坏。

# 社区报告的漏洞修复

- **WG-63881** 已修复：在更改音乐对象的输出总线时发生崩溃。
- **WG-69402** 已修复：相对于 WwiseConsole.exe 而言，有些使用 Wwise.exe 生成的 `.bnk` 和 `.wem` 文件存在二进制差异。WwiseConsole.exe 生成的文件保持不变，Wwise.exe 生成的文件则略有不同。
- **WG-71833** 已修复：(Mastering Suite) 多声道音频对象导致增益估算出错。
- **WG-71852** 已修复：位置编辑器属性播放类型同步不正确。
- **WG-72796** 已修复：(macOS) EULA 消息框有时不显示任何内容。
- **WG-72997** 已修复：当 3D 音频处于活动状态时，Apple 空间音频的 HRTF 不应用于空间化源。
- **WG-73182** 已修复：使用在 Profile with asserts 构建配置中构建的音频引擎的游戏连接 Wwise Profiler 时，大多数状态会触发断言。
- **WG-73257** 已修复：在使用 `AK::SoundEngine::SetSpeakerAngles` 时，ComputeVBAP3D 发生崩溃。
- **WG-73411** 已修复：加载自定义命令文件时可能崩溃。
- **WG-73451** 已修复：在 WAAPI 导入期间绘制列标题时可能崩溃。
- **WG-73463** 已修复：自动闪避在发布配置中首次播放时不起作用。
- **WG-73539** 已修复：(空间音频) 在混响区域内时，距离探针使用到已连接门户的距离。
- **WG-73596** 已修复：在连接到游戏时修改插件媒体（例如 IR 卷积混响）时崩溃。
- **WG-73630** 已修复：在某些编译器上 `AkBitFuncs.h` 的编译失败。
- **WG-73692** 已修复：在媒体处于在用状态时添加它的多个副本会导致 `AkMediaEntry::Release` 发生崩溃或触发断言。
- **WG-73708** 已修复：在使用 PCM 编解码器时，`Bypass_I16_2ChanAVX2` 发生崩溃。
- **WG-73849** 已修复：在更改 Project Settings 或生成 SoundBank 后 [ak.wwise.core.getProjectInfo](ak_wwise_core_getprojectinfo.html) 没有提供正确的 SoundBank 路径。