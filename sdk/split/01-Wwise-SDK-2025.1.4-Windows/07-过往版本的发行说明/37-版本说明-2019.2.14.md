# 版本说明 2019.2.14

|  |
| --- |
| Wwise SDK 2025.1.4 - Windows |

版本说明 2019.2.14

以下各节列举并阐述了 2019.2.13 和 2019.2.14 版本之间针对 Wwise 所作的改进。  
此处提供了有关平台的特定信息：

[Windows 和 Windows UWP 2019.2.14](windows_releasenotes_2019_2_14.html)

有关早期版本的信息，请参阅 [过往版本的发行说明](previousreleases.html) 章节。

- [性能改进](whatsnew_2019_2_14.html#generic_perf_changes_2019_2_14)
- [其他改进](whatsnew_2019_2_14.html#generic_misc_2019_2_14)
- [漏洞修复](whatsnew_2019_2_14.html#generic_bugs_2019_2_14)
- [社区报告的漏洞修复](whatsnew_2019_2_14.html#generic_community_bugs_2019_2_14)

# 性能改进

- **WG-56533** 提升了由全局作用域 RTPC 驱动的 Switch Container 的性能。

# 其他改进

- **WG-57325** 取消了对 Unreal Engine 4.25 的支持。现在支持的最低版本为 4.26。
- **WG-57503** 取消了 Magic Leap (Lumin) 对 Wwise SDK 的支持。
- **WG-57526** Wwise 现在支持 Windows 11。

# 漏洞修复

- **WG-55883** 已修复：在使用 File Manager 移动源文件后媒体转码出现错误。
- **WG-56196** 已修复：在 Wwise 关闭时，Advanced Profiler 视图可能会发生崩溃。
- **WG-56201** 已修复：在所有活跃输出均为二路输出的情况下，没有在调用 `AK::SoundEngine::WakeupFromSuspend` 之后发送 `AkGlobalCallbackLocation_WakeupFromSuspend` 回调。
- **WG-56377** 已修复：使用了 32 位扇区 `StreamMgrModule.h::AkFileDesc::uSector` 保存文件包条目。
- **WG-56454** 已修复：(wp.py) 在数据包创建过程中，数据包命令导致长文件路径被截断。
- **WG-56508** 已修复：在播放零延迟声音时，Opus 播放流出现匮乏。
- **WG-56879** 已修复：(wp.py) 对于采用不同配置的 Authoring 条目，`generate-bundle` 操作赋予了 `"name"` 字段完全相同的值。

# 社区报告的漏洞修复

- **WG-55531** 已修复：有时会从 Work Unit 文件中移除 MIDI 文件源引用。
- **WG-56195** 已修复：在打开损坏的性能分析会话时可能会发生崩溃。
- **WG-56346** 已修复：播放文件头大于流播放 I/O 粒度的流媒体（比如有些较长的 WEM Opus 文件）会导致发生 "File header too large" 错误。
- **WG-56351** 已修复：在驱动 Blend Container 的 Modulator 具有受 RTPC 控制的属性时，`CAkRTPCMgr::AkRTPCSubscription::PushUpdate` 引发崩溃。
- **WG-56645** 已修复：在从之前的解码错误恢复时重置 XMA 硬件导致 GDK 发生崩溃。
- **WG-56664** 已修复：在参数由 MIDI Velocity 驱动时触发断言并引发崩溃。
- **WG-56680** 已修复：在加载大量 Switch Container 且 RTPC 驱动的 Switch 的值发生变化时性能出现下降。
- **WG-56717** 已修复：`CAkParameterTarget::SetRootNode` 函数可能会触发错误的断言。
- **WG-57194** 已修复：由于在主机上安装了 CRT，导致 SoundBanks.xml 数据中的 MaxAttenuation 和 MaxDuration 信息出现舍入差异。
- **WG-57204** 已修复：在使用 XMA 的情况下通过内存指针而非使用 SoundBank ID 或文件名卸载 SoundBank 时发生崩溃。