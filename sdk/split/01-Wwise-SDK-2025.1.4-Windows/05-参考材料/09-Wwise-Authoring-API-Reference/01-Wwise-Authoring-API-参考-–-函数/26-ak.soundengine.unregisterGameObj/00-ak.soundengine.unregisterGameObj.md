# ak.soundengine.unregisterGameObj

|  |
| --- |
| Wwise SDK 2025.1.4 - Windows |

ak.soundengine.unregisterGameObj

注销游戏对象。将游戏对象注册两次并没有什么作用。注销一次便可将其彻底注销，无论其之前被注册过多少次。允许注销在使用的游戏对象，但是会失去对其参数的控制。比方说，与此游戏对象关联的声音是在 3D 空间内移动的声音。在注销游戏对象后其会停止移动，而且无法恢复对它的控制。参阅 `AK::SoundEngine::UnregisterGameObj` 章节。

# 参数

| 名称 | 类型 | 说明 |
| --- | --- | --- |
| **gameObject** \* | integer | 所要注销的游戏对象的 ID。Valid range is [0, 0xFFFFFFFFFFFFFFDF]. 可使用 `AK_INVALID_GAME_OBJECT` 来注销所有游戏对象。  游戏对象 ID，64 位无符号整数。范围：[0,18446744073709551615] |

(必填 \*)

[参数架构](ak_soundengine_unregistergameobj_args_schema.html)

# 另请参阅

- [ak.soundengine.registerGameObj](ak_soundengine_registergameobj.html)

# 示例

- [注销游戏对象](ak_soundengine_unregistergameobj_example_unregistering_a_game_object.html)

有关 Wwise Authoring API 的详细信息，请参阅 [使用 Wwise Authoring API（WAAPI）](waapi.html) 章节。